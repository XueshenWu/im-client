name: Release Build

on:
  push:
    branches:
      - release

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for Linux
        run: npm run build:linux:no-check

      - name: Copy artifacts to runner storage
        run: |
          ARTIFACT_DIR="/opt/artifacts/${{ github.sha }}"
          mkdir -p "$ARTIFACT_DIR"
          cp release/*.AppImage "$ARTIFACT_DIR/" 2>/dev/null || true
          cp release/*.deb "$ARTIFACT_DIR/" 2>/dev/null || true
          cp release/*.rpm "$ARTIFACT_DIR/" 2>/dev/null || true
